import './main.scss';
console.log(`CSS reloads with HMR ${new Date().toISOString()}`);

// const bg = `url("${getSnowSvgDataUrl()}") !important`;

// console.log("Background", bg);

// setTimeout(() => {
//   document.body.style.backgroundImage = bg;
// }, 2000)